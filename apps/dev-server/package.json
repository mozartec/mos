{
  "name": "@mos/dev-server",
  "version": "0.0.0",
  "description": "Dev filesystem server backing HttpVaultSource. Lists and reads vault files over HTTP; exposes a change-event stream (SSE). Used only during local development (ADR-006).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/bun": "^1.2.17",
    "eslint": "^10.3.0",
    "typescript": "~6.0.2",
    "typescript-eslint": "8.60.1"
  }
}
