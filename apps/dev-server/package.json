{
  "name": "@mos/dev-server",
  "version": "0.0.0",
  "description": "Dev filesystem server backing HttpVaultSource: a thin Bun wrapper around the shared @mos/vault-server endpoints. Used only during local development (ADR-006).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "check-types": "tsc --noEmit",
    "test": "echo 'no tests here — endpoint and watcher logic is tested in @mos/vault-server'"
  },
  "dependencies": {
    "@mos/vault-server": "workspace:*"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/bun": "^1.2.17",
    "eslint": "^10.3.0",
    "typescript": "~6.0.2",
    "typescript-eslint": "8.60.1"
  }
}
